server:
  port: 8080
  context-path: /sq

quartz:
  enabled: true
spring:
  thymeleaf:
    mode: LEGACYHTML5
  http:
    multipart:  # 文件上传
      enabled: true
      max-file-size: 50MB #单个数据大小
      max-request-size: 100MB #总数据大小
  profiles:
     active: qzxdev
mybatis:
  configuration:
    mapUnderscoreToCamelCase: true

version:
  pcversion: "V1.3" # 版本号
  build: 1113 # 小版本号
